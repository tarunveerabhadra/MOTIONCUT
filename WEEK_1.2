import java.util.Random;
import java.util.Scanner;

public class FakeJobTitleGenerator {

    public static void main(String[] args) {
       
        String[] adjectives = {
            "Quantum", "Digital", "Virtual", "Dynamic", "Agile",
            "Synergistic", "Futuristic", "Invisible", "Strategic", "Smart"
        };

        String[] jobTitles = {
            "Marketing Ninja", "Data Wrangler", "Synergy Officer",
            "Innovation Evangelist", "Brand Guru", "Cloud Shepherd",
            "AI Whisperer", "Buzzword Analyst", "Scrum Prophet", "Engagement Hacker"
        };

                Random random = new Random();
        Scanner scanner = new Scanner(System.in);

        String userChoice;

        do {
            
            String adjective = adjectives[random.nextInt(adjectives.length)];
            String jobTitle = jobTitles[random.nextInt(jobTitles.length)];

            String fullTitle = adjective + " " + jobTitle;

                        System.out.println("\n🚀 Your Fake Job Title: " + fullTitle);

                       System.out.print("Want another one? (yes/no): ");
            userChoice = scanner.nextLine().toLowerCase().trim();

        } while (userChoice.equals("yes") || userChoice.equals("y"));

        System.out.println("\nThanks for generating nonsense titles with us! 😄");

        scanner.close();
    }
}
